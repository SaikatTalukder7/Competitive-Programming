#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    for (int j = 0; j < t; j++)
    {
        int n, m;
        long long a, b;
        cin >> n >> m >> a >> b;

        for (int i = 0; i < n; i++)
        {
            int temp;
            cin >> temp;
        }

        for (int i = 0; i < m; i++)
        {
            int temp;
            cin >> temp;
        }

        cout << n + m << endl;
    }
    return 0;
}

